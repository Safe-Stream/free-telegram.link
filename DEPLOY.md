# 🚀 ПОШАГОВАЯ ИНСТРУКЦИЯ ПО РАЗВЕРТЫВАНИЮ

## ⚡ Быстрая установка (3 команды)

```bash
# 1. Клонируем репозиторий
cd /opt
git clone https://github.com/Safe-Stream/free-telegram.link.git
cd free-telegram.link

# 2. Запускаем установку
chmod +x install.sh
./install.sh
```

Скрипт автоматически установит Docker, получит SSL сертификат и запустит сервисы.

---

## Полная инструкция

## Этап 1: Подготовка сервера (5 минут)

### 1.1 Подключение к серверу
```bash
ssh root@62.133.61.144
```

### 1.2 Обновление системы
```bash
apt update && apt upgrade -y
```

### 1.3 Установка Git
```bash
apt install -y git curl wget
```

## Этап 2: Клонирование репозитория (1 минута)

```bash
cd /opt
git clone https://github.com/Safe-Stream/free-telegram.link.git
cd free-telegram.link
```

## Этап 3: Запуск установки (10-15 минут)

### 3.1 Сделать скрипт исполняемым
```bash
chmod +x install.sh update.sh
```

### 3.2 Запустить установку
```bash
./install.sh
```

**Скрипт попросит ввести:**
- Email для Let's Encrypt (например: admin@safe-stream.net)

**Что делает скрипт автоматически:**
1. ✓ Устанавливает Docker и Docker Compose
2. ✓ Генерирует SECRET для MTProxy
3. ✓ Получает SSL сертификат
4. ✓ Загружает Docker образы из GitHub Container Registry
5. ✓ Настраивает и запускает все контейнеры
6. ✓ Выводит готовые ссылки для подключения

## Этап 4: Проверка работы (2 минуты)

### 4.1 Проверить статус контейнеров
```bash
docker-compose ps
```

**Должны работать:**
- `telegram-mtproxy` (Up)
- `telegram-nginx` (Up)
- `telegram-certbot` (Up)

### 4.2 Открыть сайт в браузере
```
https://free-telegram.link
```

### 4.3 Проверить прокси
Нажмите кнопку "Открыть Telegram" на сайте - должен открыться Telegram

## Этап 5: Получение ссылок

### 5.1 Просмотреть сохраненные ссылки
```bash
cat mtproxy-config.txt
```

**Вы увидите:**
```
MTProxy Configuration
=====================
SECRET: a1b2c3d4e5f6...
SERVER_IP: 62.133.61.144
PORT: 443

Telegram Links:
- Direct: tg://proxy?server=62.133.61.144&port=443&secret=...
- Universal: https://t.me/proxy?server=...
```

### 5.2 Использование ссылок

**Для HTML-страницы:**
Ссылка уже вставлена автоматически в `config/index.html`

**Для распространения:**
- **Direct link** - для встраивания в HTML/приложения
- **Universal link** - для отправки пользователям в сообщениях

## Этап 6: Обновление сервиса 🔄

### Когда выходит обновление:

```bash
cd /opt/free-telegram.link
./update.sh
```

Скрипт автоматически:
1. Обновит код из GitHub
2. Загрузит новые Docker образы
3. Перезапустит сервисы

### Ручное обновление:

```bash
git pull
docker-compose pull
docker-compose down && docker-compose up -d
```

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### Безопасность
1. **Firewall**: Убедитесь что открыты только нужные порты
```bash
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable
```

2. **SSH**: Рекомендуется изменить порт SSH
```bash
nano /etc/ssh/sshd_config
# Измените Port 22 на другой (например 2222)
systemctl restart sshd
```

### Мониторинг
1. **Проверка логов MTProxy:**
```bash
docker-compose logs -f mtproxy
```

2. **Проверка логов Nginx:**
```bash
tail -f /opt/telegram-proxy/logs/access.log
```

### Обновление SSL сертификата
Certbot автоматически обновляет сертификаты каждые 12 часов.

**Ручное обновление (если нужно):**
```bash
docker-compose run --rm certbot renew
docker-compose restart nginx
```

## 🐛 Устранение проблем

### Проблема 1: Не получается SSL сертификат
**Причина:** DNS не обновился или домен не указывает на сервер

**Решение:**
```bash
# Проверьте DNS
dig free-telegram.link +short
# Должен показать: 62.133.61.144

# Если нет - подождите до 24 часов пока обновится DNS
# Или временно используйте без HTTPS (только для тестирования)
```

### Проблема 2: Порт 443 занят
**Причина:** На сервере уже что-то использует порт 443

**Решение:**
```bash
# Проверьте что использует порт
netstat -tulpn | grep 443

# Остановите конфликтующий сервис
# Например Apache:
systemctl stop apache2
```

### Проблема 3: Контейнеры не запускаются
**Решение:**
```bash
# Посмотрите логи
docker-compose logs

# Пересоздайте контейнеры
docker-compose down
docker-compose up -d --force-recreate
```

## ✅ ЧЕКЛИСТ ПОСЛЕ УСТАНОВКИ

- [ ] Все 3 контейнера работают (docker-compose ps)
- [ ] Сайт открывается по https://free-telegram.link
- [ ] Кнопка "Открыть Telegram" работает
- [ ] SSL сертификат валидный (зеленый замок в браузере)
- [ ] MTProxy принимает соединения

## 📞 ПОДДЕРЖКА

Если что-то не работает:
1. Проверьте логи: `docker-compose logs`
2. Убедитесь что DNS настроен правильно
3. Проверьте firewall
4. Свяжитесь через [@Safe_Stream_bot](https://t.me/Safe_Stream_bot)

---

**Время полной установки:** ~20 минут  
**Сложность:** Средняя (большинство действий автоматизировано)
