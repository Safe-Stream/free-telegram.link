# 📤 Инструкция по загрузке проекта в GitHub

## Шаг 1: Инициализация Git репозитория

В PowerShell на вашем Windows ПК:

```powershell
cd C:\telegram-proxy

# Инициализируем Git (если еще не сделано)
git init

# Добавляем все файлы
git add .

# Делаем первый коммит
git commit -m "Initial commit: Telegram MTProxy with web interface"
```

## Шаг 2: Подключение к GitHub репозиторию

```powershell
# Добавляем удаленный репозиторий
git remote add origin https://github.com/Safe-Stream/free-telegram.link.git

# Устанавливаем главную ветку
git branch -M main

# Отправляем код в GitHub
git push -u origin main
```

**Примечание:** При первой отправке GitHub может попросить авторизацию:
- Используйте Personal Access Token (PAT) вместо пароля
- Создайте токен: Settings → Developer settings → Personal access tokens → Generate new token
- Права: `repo`, `write:packages`, `read:packages`

## Шаг 3: Настройка GitHub Actions

После первого push GitHub Actions автоматически:
1. Соберет Docker образы для Nginx и MTProxy
2. Опубликует их в GitHub Container Registry
3. Образы станут доступны по адресам:
   - `ghcr.io/safe-stream/free-telegram-nginx:latest`
   - `ghcr.io/safe-stream/free-telegram-mtproxy:latest`

### Проверка сборки

1. Перейдите на GitHub: https://github.com/Safe-Stream/free-telegram.link
2. Откройте вкладку **Actions**
3. Дождитесь завершения workflow "Build and Push Docker Images"
4. Статус должен быть ✅ (зеленая галочка)

### Просмотр образов

1. Перейдите на главную страницу репозитория
2. Справа в разделе **Packages** увидите созданные образы
3. Или перейдите напрямую: https://github.com/orgs/Safe-Stream/packages

## Шаг 4: Настройка публичного доступа к образам

По умолчанию образы приватные. Чтобы сделать их публичными:

1. Перейдите в Packages → выберите образ (например, `free-telegram-nginx`)
2. Settings (справа) → Danger Zone → Change visibility
3. Выберите **Public**
4. Повторите для второго образа (`free-telegram-mtproxy`)

## Шаг 5: Развертывание на сервере

Теперь на сервере можно установить всё одной командой:

```bash
ssh root@62.133.61.144

# Клонируем репозиторий
cd /opt
git clone https://github.com/Safe-Stream/free-telegram.link.git
cd free-telegram.link

# Запускаем установку
chmod +x install.sh
./install.sh
```

## Шаг 6: Обновление в будущем

### На локальном ПК (Windows):

Когда нужно обновить код:

```powershell
cd C:\telegram-proxy

# Вносим изменения в файлы...

# Коммитим изменения
git add .
git commit -m "Описание изменений"
git push
```

GitHub Actions автоматически пересоберет образы!

### На сервере:

```bash
cd /opt/free-telegram.link
./update.sh
```

Скрипт автоматически:
- Обновит код (`git pull`)
- Загрузит новые образы
- Перезапустит сервисы

## 🔐 Безопасность

### Файлы, которые НЕ попадут в Git (уже в .gitignore):

- ✅ `.env` - переменные окружения с секретами
- ✅ `mtproxy-config.txt` - конфигурация с SECRET
- ✅ `certbot/conf/` - SSL сертификаты
- ✅ `logs/` - логи

### Файлы, которые БУДУТ в репозитории:

- ✅ Все конфигурационные файлы
- ✅ Dockerfile'ы
- ✅ docker-compose.yml
- ✅ Скрипты установки
- ✅ HTML/CSS/JS файлы

## 📊 Мониторинг CI/CD

После каждого `git push`:

1. **GitHub Actions** запускается автоматически
2. Собирает Docker образы
3. Тестирует сборку
4. Публикует в Container Registry
5. Отправляет уведомления (можно настроить)

### Просмотр логов сборки:

1. GitHub → Actions → выбрать последний workflow
2. Развернуть "Build and push Nginx image" или "Build and push MTProxy image"
3. Посмотреть детали сборки

## ⚡ Быстрые команды

```powershell
# Проверка статуса Git
git status

# Просмотр истории коммитов
git log --oneline

# Откатить изменения (если что-то пошло не так)
git reset --hard HEAD

# Обновить с GitHub (если делали изменения через веб-интерфейс)
git pull

# Создать новую ветку для экспериментов
git checkout -b feature/new-design
git push -u origin feature/new-design
```

## 🎉 Готово!

Теперь у вас:
- ✅ Код в GitHub репозитории
- ✅ Автоматическая сборка образов
- ✅ Простое развертывание одной командой
- ✅ Обновление через `git pull` + `./update.sh`

---

**Время настройки:** ~10 минут  
**Необходимые права:** Admin в репозитории Safe-Stream
