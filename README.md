# Telegram MTProxy + Веб-страница для free-telegram.link

[![Build Status](https://github.com/Safe-Stream/free-telegram.link/actions/workflows/docker-build.yml/badge.svg)](https://github.com/Safe-Stream/free-telegram.link/actions)
[![Docker Pulls](https://img.shields.io/badge/docker-ghcr.io-blue)](https://github.com/orgs/Safe-Stream/packages)

Профессиональное решение для предоставления бесплатного доступа к Telegram через MTProxy с современным веб-интерфейсом. Автоматическая сборка и публикация Docker образов через GitHub Actions.

## 🚀 Быстрый старт

### Требования
- Ubuntu 20.04+ / Debian 11+
- Root доступ
- Домен `free-telegram.link` с A-записью на IP сервера
- Открытые порты: 80, 443

### Установка за 3 команды

```bash
# 1. Клонируем репозиторий
cd /opt
git clone https://github.com/Safe-Stream/free-telegram.link.git
cd free-telegram.link

# 2. Делаем скрипт исполняемым
chmod +x install.sh update.sh

# 3. Запускаем установку
./install.sh
```

Скрипт автоматически:
- ✅ Установит Docker и Docker Compose
- ✅ Сгенерирует SECRET для MTProxy
- ✅ Получит SSL сертификат
- ✅ Загрузит образы из GitHub Container Registry
- ✅ Настроит и запустит все сервисы

## 📦 Docker образы

Проект использует готовые образы из GitHub Container Registry:

- `ghcr.io/safe-stream/free-telegram-nginx:latest` - Nginx с веб-страницей
- `ghcr.io/safe-stream/free-telegram-mtproxy:latest` - MTProxy сервер

Образы автоматически собираются при каждом коммите в `main` ветку через GitHub Actions.

## 📦 Структура проекта

```
telegram-proxy/
├── docker-compose.yml       # Оркестрация контейнеров
├── install.sh              # Скрипт автоматической установки
├── generate-secret.sh      # Генератор SECRET (альтернативный)
├── nginx/
│   ├── Dockerfile          # Образ Nginx
│   ├── nginx.conf          # Конфигурация Nginx
│   └── html/
│       └── index.html      # Веб-страница в стиле Safe-Stream
├── certbot/
│   ├── conf/               # SSL сертификаты
│   └── www/                # ACME challenge
└── logs/                   # Логи Nginx
```

## 🔧 Сервисы

### 1. MTProxy
- **Порт**: 443
- **Протокол**: MTProto
- **Назначение**: Прокси-сервер для Telegram

### 2. Nginx
- **Порты**: 80 (HTTP), 8443 (HTTPS)
- **Назначение**: Веб-сервер для страницы подключения

### 3. Certbot
- **Назначение**: Автоматическое обновление SSL сертификатов

## 🛠️ Управление

### Проверка статуса
```bash
docker-compose ps
```

### Просмотр логов
```bash
# Все сервисы
docker-compose logs -f

# Только MTProxy
docker-compose logs -f mtproxy

# Только Nginx
docker-compose logs -f nginx
```

### Перезапуск сервисов
```bash
docker-compose restart
```

### Остановка
```bash
docker-compose down
```

### Полный перезапуск с пересборкой
```bash
docker-compose down
docker-compose up --build -d
```

## 🔐 Получение ссылки на прокси

После установки ссылка сохранена в:
```bash
cat /opt/telegram-proxy/mtproxy-config.txt
```

Или сгенерируйте новую:
```bash
./generate-secret.sh
```

## 🌐 Веб-страница

Страница доступна по адресу:
- **HTTPS**: https://free-telegram.link
- **HTTP**: http://free-telegram.link (редирект на HTTPS)

### Особенности дизайна:
- Минималистичный стиль в духе Safe-Stream
- Адаптивная вёрстка (мобильные + десктоп)
- Предупреждение об отключении VPN
- Ссылка на Telegram-бот Safe Stream

## 📝 Обновление ссылки прокси

Если нужно изменить ссылку на прокси в HTML:

1. Откройте файл:
```bash
nano /opt/telegram-proxy/nginx/html/index.html
```

2. Найдите строку:
```html
<a href="tg://proxy?server=...&port=443&secret=..." class="btn-primary">
```

3. Замените на новую ссылку

4. Перезапустите Nginx:
```bash
docker-compose restart nginx
```

## 🔒 SSL/TLS

Certbot автоматически обновляет сертификаты каждые 12 часов.

### Ручное обновление:
```bash
docker-compose run --rm certbot renew
docker-compose restart nginx
```

## 🐛 Устранение неполадок

### Проблема: MTProxy не запускается
```bash
docker-compose logs mtproxy
# Проверьте, что порт 443 свободен
netstat -tulpn | grep 443
```

### Проблема: Не получается SSL сертификат
1. Убедитесь, что домен указывает на сервер:
```bash
dig free-telegram.link +short
```

2. Проверьте, что порт 80 доступен:
```bash
curl -I http://free-telegram.link
```

### Проблема: Сайт недоступен
```bash
# Проверка Nginx
docker-compose logs nginx

# Проверка конфигурации
docker-compose exec nginx nginx -t
```

## 📊 Мониторинг

### Проверка работы MTProxy:
```bash
# Должен показывать активные соединения
docker-compose exec mtproxy netstat -an | grep 443
```

### Статистика использования:
```bash
# Количество подключений
docker-compose logs mtproxy | grep -c "connection"
```

## 🔄 Обновление

Чтобы обновить сервис до последней версии:

```bash
cd /opt/free-telegram.link
./update.sh
```

Скрипт автоматически:
1. Обновит код из GitHub (`git pull`)
2. Загрузит новые Docker образы
3. Перезапустит сервисы с zero-downtime

### Ручное обновление

```bash
# Обновить код
git pull

# Загрузить новые образы
docker-compose pull

# Перезапустить
docker-compose down && docker-compose up -d
```

## 💡 Рекомендации

1. **Регулярное резервное копирование**:
```bash
tar -czf telegram-proxy-backup-$(date +%Y%m%d).tar.gz /opt/telegram-proxy
```

2. **Мониторинг логов**:
Настройте logrotate для логов Nginx

3. **Firewall**:
Убедитесь, что открыты только необходимые порты:
```bash
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable
```

## 📞 Поддержка

Для вопросов и проблем:
- Telegram бот: [@Safe_Stream_bot](https://t.me/Safe_Stream_bot)
- Основной сайт: https://safe-stream.net

## 📄 Лицензия

Проект разработан для Safe Stream VPN
